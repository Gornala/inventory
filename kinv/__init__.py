"""kinv — KiCad BOM to consolidated inventory to a basket per supplier.

Standard library only, on purpose: it runs on any Python 3.11 or newer,
including the one KiCad ships in its own ``bin`` directory, with nothing to
install.
"""

__version__ = "0.1.0"
